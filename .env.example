# Скопируй в .env и подставь свои значения.
# БД приложения (MySQL/MariaDB). НЕ боевая база сайта — отдельная схема.
DATABASE_URL="mysql://user:password@localhost:3306/zenair_app"

# Порт API (по умолчанию 3001)
PORT=3001
